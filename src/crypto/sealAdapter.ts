import type { SealAdapter } from "../types";

export const sealAdapter: SealAdapter = {
  async encrypt(value) {
    // TODO: Replace with a real Seal implementation and key management flow.
    return value;
  },
  async decrypt(value) {
    // TODO: Replace with a real Seal implementation and key management flow.
    return value;
  },
};
